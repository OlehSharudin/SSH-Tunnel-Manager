# SSH-Tunnel-Manager
A cross-platform GUI tool (Python/Tkinter) for creating and managing SSH tunnels through bastion hosts. Supports multiple simultaneous tunnels, save/load configurations, and auto-connect on startup.
